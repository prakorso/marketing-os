// Test-only stub for the `server-only` package, aliased in vitest.config.ts
// (integration tests ONLY — see the alias comment there for why this exists
// and why it does not weaken production protection).
export {};
